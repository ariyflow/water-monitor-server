[project]
name = "water-monitor-server-v0-0"
version = "0.1.0"
description = "Water quality monitor backend server"
readme = "README.md"
authors = [
    { name = "ariyflow", email = "ariyflow4342@gmail.com" }
]
requires-python = ">=3.12"
dependencies = [
    "flask>=3.1.3",
]

[tool.uv]
package = false
